/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["sangw.in", "localhost", "picsum.photos", "gstatic.com"], // <== Domain name
  },
};

export default nextConfig;
